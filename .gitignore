# Dependencies
node_modules/

# Build output
dist/
dev-dist/
coverage/
*.tsbuildinfo

# Editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# Local secrets (Google OAuth client ID, GAS web app URL) — see .env.example
.env
.env.local

# Logs
npm-debug.log*
*.log

# Copyrighted / institutional source material — never publish (see AUDIT.md S-1)
uploads/
